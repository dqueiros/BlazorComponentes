@using static CascataUsuarioPai

<h3>Usuario Filho</h3>

@if (UsuarioAtual?.Autenticado == true)
{
    <p>Bem-vindo, @UsuarioAtual.Nome!</p>
}
else
{
    <p>Usuário não autenticao</p>
}

@code {
    [CascadingParameter]
    public Usuario? UsuarioAtual { get; set; }
}